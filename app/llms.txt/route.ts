import { categories, publishedJobs } from "@/content/jobs";import { absoluteUrl } from "@/lib/site";
export const dynamic="force-static";
export function GET(){const lines=["# manapick career","","> なりたい仕事まで、迷わせない。職業の仕事内容・必要スキル・学ぶ順番を公式情報から整理します。","","## Published careers",...publishedJobs.map(job=>`- [${job.name}](${absoluteUrl(`/career/${job.slug}/`)}): ${job.conclusion[0]}`),"","## Categories",...categories.map(x=>`- [${x.label}](${absoluteUrl(`/category/${x.key}/`)}): ${x.description}`),"","## Policies",`- [作成方法・編集方針](${absoluteUrl("/about-method/")})`,`- [免責事項](${absoluteUrl("/disclaimer/")})`];return new Response(lines.join("\n"),{headers:{"Content-Type":"text/plain; charset=utf-8"}})}
