import fs from "node:fs/promises";import path from "node:path";import {fileURLToPath} from "node:url";
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),"..");const network=JSON.parse(await fs.readFile(path.join(root,"content/network-map.json"),"utf8"));const args=new Set(process.argv.slice(2));
if(args.has("--internal")){const out=path.join(root,"out");const html=[];async function walk(dir){for(const entry of await fs.readdir(dir,{withFileTypes:true})){const full=path.join(dir,entry.name);if(entry.isDirectory())await walk(full);else if(entry.name.endsWith(".html"))html.push(full)}}await walk(out);const missing=[];for(const file of html){const body=await fs.readFile(file,"utf8");for(const match of body.matchAll(/href="(\/[^"]*)"/g)){const raw=match[1];if(raw.startsWith("//"))continue;const pathname=decodeURI(raw.split(/[?#]/)[0]);if(!pathname||pathname==="/")continue;const target=pathname.endsWith("/")?path.join(out,pathname,"index.html"):path.join(out,pathname);if(!await fs.stat(target).then(()=>true).catch(()=>false))missing.push(`${path.relative(out,file)} -> ${raw}`)}}if(missing.length){missing.forEach(x=>console.error(`ERROR ${x}`));process.exit(1)}console.log(`internal links: ${html.length} HTML files; 0 missing`)}
if(args.has("--network")){const report=[];for(const [itemId,item] of Object.entries(network.items)){const url=new URL(item.path,`${network.sites[item.siteId].baseUrl}/`).toString();let status=0;let state="unconfirmed";try{let res=await fetch(url,{method:"HEAD",redirect:"follow",headers:{"user-agent":"manapick-career-link-audit/1.0"}});if(res.status===405)res=await fetch(url,{method:"GET",redirect:"follow",headers:{"user-agent":"manapick-career-link-audit/1.0","range":"bytes=0-0"}});status=res.status;state=status>=200&&status<400?"ok":status===404?"broken":status===403||status===429?"bot-restricted":"unconfirmed"}catch{state="unconfirmed"}report.push({itemId,url,status,state})}await fs.mkdir(path.join(root,"docs"),{recursive:true});await fs.writeFile(path.join(root,"docs/qa-network.json"),JSON.stringify({checkedAt:new Date().toISOString(),report},null,2)+"\n");const broken=report.filter(x=>x.state==="broken");console.log(`network links: ${report.filter(x=>x.state==="ok").length} ok / ${broken.length} broken / ${report.filter(x=>x.state==="bot-restricted").length} bot-restricted / ${report.filter(x=>x.state==="unconfirmed").length} unconfirmed`);if(broken.length)process.exit(1)}
